# Supabase Configuration
# Get these values from your Supabase project dashboard at https://app.supabase.com
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-public-key-here

# Optional: Supabase Project ID (for additional integrations)
VITE_SUPABASE_PROJECT_ID=your-project-id

# Development Configuration
# Uncomment and set these for local development overrides if needed
# VITE_API_BASE_URL=http://localhost:3000
# VITE_DEBUG_MODE=true

# Note: The VITE_ prefix is required for Vite to expose these variables to the client-side code
# These are public keys that are safe to expose in the client bundle
# Sensitive keys should be handled server-side only