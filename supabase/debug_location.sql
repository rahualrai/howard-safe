
-- Check friendships
SELECT * FROM friendships;

-- Check location sharing preferences
SELECT * FROM location_sharing_preferences;

-- Check user locations
SELECT * FROM user_locations;

-- Check what the function returns for a specific user (replace UUID with actual user ID if known, or just run it for one of the users found above)
-- SELECT * FROM get_friends_with_locations('USER_UUID_HERE');
